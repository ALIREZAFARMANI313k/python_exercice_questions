Author:alireza farmani

# تعریف تابع معادله دیفرانسیل

:)def diff_eq(y, x
# معادله دیفرانسیل اولین مرتبه: y ,x(f' = y)

))" :return eval(input("Enter the differential equation in terms of y and x
# شرط اولیه

))" :)y0 = float(input("Enter the initial condition y(0
# محاسبه مقادیر ی

:)def solve_diff_eq(diff_eq, y0, x_values
]y_values = [y0
:))for i in range(1, len(x_values
]delta_x = x_values[i] - x_values[i-1
delta_y = diff_eq(y_values[-1], x_values[i-1]) * delta_x
y_next = y_values[-1] + delta_y
)y_values.append(y_next
return y_values
# بازهی زمانی

])x_values = [i for i in range(0, 11
# حل معادله دیفرانسیل

)y_values = solve_diff_eq(diff_eq, y0, x_values
# چاپ نتیجه

)":print("Solution
)print(y_values
